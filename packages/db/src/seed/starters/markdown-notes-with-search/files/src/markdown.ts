// Sprint 1, task 3: write and render markdown side by side.
//
// `marked` is installed. Read what it does with raw HTML before you trust it
// with a note somebody pasted from the internet.

/** Markdown in, HTML out. Raw HTML in the note must come out as text, never as markup. */
export function renderMarkdown(source: string): string {
  throw new Error("TODO: sprint 1, task 3");
}
