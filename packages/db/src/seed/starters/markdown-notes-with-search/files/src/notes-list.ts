// Sprint 1, task 5: list, open and delete notes.
import type { Note } from "./note";

/** The note's first heading; else its first non-empty line; else "Untitled". */
export function titleOf(note: Note): string {
  throw new Error("TODO: sprint 1, task 5");
}

/** Most recently updated first. Returns a new array. */
export function sortByUpdated(notes: readonly Note[]): Note[] {
  throw new Error("TODO: sprint 1, task 5");
}
