import type { Testimonial } from "./types"

/**
 * Testimonials from placement cells and faculty using ShipItHQ University (/uni).
 * Format and rules: ./types.ts. Empty until real quotes arrive; the wall stays hidden
 * below MIN_TESTIMONIALS entries.
 */
export const TESTIMONIALS: Testimonial[] = []
