import { RoundsOverviewSkeleton } from "@/components/hiring/rounds-overview-skeleton"

/** Most visits are to a built job, so the skeleton is its rounds page. */
export default function Loading() {
    return <RoundsOverviewSkeleton />
}
