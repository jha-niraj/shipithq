// Hand-matched to the module page (plan/practice-ui, UI-3). The practice tab
// row is in the layout, so it stays on screen while this shows.
import { ModuleContentSkeleton } from "../_components/module-content";

export default function Loading() {
    return <ModuleContentSkeleton />;
}
