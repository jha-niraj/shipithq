"use client"

// Shared with apps/hiring's shell (plan/hiring-app HA-1). The main app's
// presets are passed in by its sidebar wrapper.
export { CustomizeSidebarSheet } from "@repo/ui/components/shell/customize-sidebar-sheet"
