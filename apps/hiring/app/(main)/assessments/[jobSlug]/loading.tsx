// /assessments/[jobSlug] only redirects to /assignments/[jobSlug], so the brief
// wait before the redirect shows the skeleton of the page it lands on.
export { default } from "../../assignments/[jobSlug]/loading";
